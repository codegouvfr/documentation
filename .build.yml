image: debian/testing
packages:
  - emacs
  - org-mode
  - rsync
  - python3
  - pipx
sources:
  - https://git.sr.ht/~codegouvfr/documentation
secrets:
  - bdd50953-add5-4ddb-9f34-9f0781900b7e
tasks:
  - make_mkdocs: |
      cd documentation/
      pipx install pipenv
      /home/build/.local/bin/pipenv install --index gitlab mkdocs-dsfr
      /home/build/.local/bin/pipenv install
      /home/build/.local/bin/pipenv shell
      make md
  - upload: |
      rsync -e "ssh -o StrictHostKeyChecking=no" -av documentation/public/* web@code.gouv.fr:/home/web/websites/documentation/
triggers:
  - action: email
    condition: failure
    to: bastien.guerry@code.gouv.fr
